# 代码在哪（四人共用）

**只有一套产品面。不要再维护 MVP 分叉。**

## 入口

| 用途 | 路径 |
|------|------|
| 产品唯一 URL | `http://127.0.0.1:3331/track/knowledge` |
| 页面 | `app/track/knowledge/page.tsx` |
| 旧 `/mvp` | `next.config.ts` 永久重定向到上面，**无独立页面代码** |

## 目录（都在 `app/track/knowledge/`）

```
page.tsx                 # 工作台壳：画布、拖入、授权空台
project-canvas.module.css
workbench-entry.module.css  # 空台/授权/过程条样式
read-drop-entries.ts
components/              # 全部 UI 组件（含授权、理解、画布）
  LocalFolderEntry.tsx   # 空台：价值先见 + 选本地夹
  EntryPreview.tsx
  AgentProcessPanel.tsx
  ProjectCanvas.tsx …
lib/
  folder-connection-api.ts   # 授权夹 connect / memory / analysis 客户端
  onboarding-folder-choice.ts
  agent-process.ts
  event-revision-open.ts
  folder-drop.ts
```

## 后端业务（共享，不双份）

| 能力 | 路径 |
|------|------|
| 文件夹授权 / connect | `shared/project-memory/` · `app/api/knowledge/project-memory/` |
| 知识项目 / 材料 / 画布 | `shared/knowledge/` · `app/api/knowledge/projects/` |
| connect 后同 id 项目 | `ensureProject` in `shared/knowledge/repository.ts` |
| 授权夹 → 画布材料桥 | `shared/knowledge/materialize-grant-signals.ts`（connect 后写入 materials + 卡片） |
| 画布图构建 | `shared/knowledge/project-canvas.ts` |
| 阶段 2 互通条 | `docs/product/PEER_NOTE_phase2.md` |

## 规范

- `docs/product/合并规范.md`
- `docs/product/首页规范.md`
- `docs/product/产品清单.md`

## 禁区（除非 Owner 改口）

- 不要新建 `app/track/knowledge/mvp/`
- 不要第二套页面入口
- 模型环文件若有人锁定，先互通再改
