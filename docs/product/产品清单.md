# 产品清单

## 怎么记（只两样）

1. **问题** — 用产品时遇到的，或必须先想清楚的
2. **结果** — 讨论后定下来要做的

做完打勾。

## 怎么合作（从简 · 无层级）

- **所有人平级。** 没有 Leader / 派发 / 汇报链。
- **你同时和所有人说话。**
- 需要时，**你点名其中一个去给别人派活**，就这一层，没有系统。
- 代码只在：`/Users/mahaoxuan/Desktop/黑客松/zhiji` · 分支 `main`
- 试用：http://127.0.0.1:3331/track/knowledge
- 夹具只认：`/tmp/mvp-v0-g6-d50-fixture`

当前窗口（全平级 · 以面板名为准）：

| 名 | surface |
|---|---|
| 全栈工程师1 | `:63` |
| 全栈工程师2 | `:78` |
| 全栈工程师3 | `:77` |
| 全栈工程师4 | `:64` |
| 全栈工程师5 | `:74` |
| **+ 你** | — |

---

## 北极星（已定）

- **形态：** Canvasight 参考图 - 左多项目 · 中关系画布 · 右摘要/动态 · 底时间线
- **喂料 ≠ 终点：** 授权夹 / 拖材料只是入口；终点是进入项目后看见真实结构并决定下一步
- **文档：** `CONTEXT.md` · `docs/product/代码在哪.md` · `docs/product/首页规范.md` · `docs/product/合并规范.md`
- **路径：** 0 对齐 → 1 四区 → 2 中心真节点（已过） → **3 右栏 Agent 过程与理解（当前主线）** → 4 时间线 → 5 多 Agent → 6 抛光

## 未完成

### 1. 真读项目夹（产品创新 · 严格把控）

- **问题：** 别人也能做「聊几句 + 假进度」。我们的差异是：在你授权的夹里，**真的**建地图 → 搜 → 读文件 → 再推理，结论要么有出处，要么老实说不知道。
- **你要看到的结果：**
  1. 授权夹之后，它先摸清目录，再搜关键词，再打开具体文件，最后才说话
  2. 右侧过程一步一步在动，不是装饰
  3. 给出的理解能点到具体文件/片段；点不开出处 = 不合格
  4. 读不动或模型挂了 → 报失败，**不许**悄悄换成假理解
  5. 你确认 / 改 / 拒绝之后，下次打开还记得
- **严格不过线（任一即 FAIL）：**
  - 没读任何真实文件就下结论
  - 有结论但没有可核对的出处
  - 页面上看不到它读了什么（只见最终一段话）
  - 模型挂了仍显示「读懂了」
  - 确认后刷新/重进就丢
- **工程（2026-07-17 整包已落地 · 等你验收）：**
  - 工具环 + 真模型默认；禁静默降级（失败 `status=failed`，不写假候选）
  - 强制可用出处：`agent-evidence`；无出处拒绝「读懂」
  - HITL 确认写回；切换项目可恢复
  - 门禁：**A1–A6 + L1–L5 全绿**（含 live L1/L2/L3）
  - 验收文：`docs/product/AGENT_PRESENCE_ACCEPTANCE.md`
  - 证据：`.ship/evidence/agent-e2e-*` · 本地 `npx vitest run shared/project-memory/` 78 passed
- **状态：** [ ] （工程完成；**你亲手**走验收清单后打勾）

### 1b. 过程看得见（为 §1 服务）

- **问题：** 若只出最终一段话，你分不清它是真读了还是瞎编。
- **结果：** 右栏始终能看到「它在做什么」：建地图 / 搜索 / 精读 / 推理 / 等你确认；状态跟真实动作走。
- **工程：** 右栏过程面板接 tool 收据；L5 断言 map/search/read 后前序步骤为 done。
- **状态：** [ ] （工程完成；你亲手看见过程在动后打勾）

### 2. 界面要像产品，不像工程台

- **问题：** 仍像验收/配置台，看不出现在怎样、下一步只决定什么；首屏只有选夹会让人不知进了什么产品。上传时页面像死机；AI Copilot 名实不符（乱跳 HTML）。
- **结果：** 打开约 3 秒内：这是什么、选完长什么样、主动作是选文件夹；确认卡在选夹后。规范：`docs/product/首页规范.md` · 合并：`docs/product/合并规范.md` · 目录：`docs/product/代码在哪.md`（唯一入口 `/track/knowledge`，无 mvp 目录）。视觉：`#007AFF` + Plus Jakarta Sans。
- **工程（2026-07-17 局部）：** 上传/接入有底部进度条（`workspace-busy-banner`）；AI Copilot = 真读夹 Agent（不跳随机材料）；「当前重点」与 Copilot 拆开。
  - 画布左上「现在怎样 / 当前请先看」不再常驻遮挡节点；摘要、建议与可点依据归入右栏概览，画布只保留按需出现的关系详情与返回动作。
  - 画布 Mini Map 默认收为左下角小缩略图，仅在悬停、键盘聚焦或触屏聚焦时展开；导航能力保留，不再长驻占据画布。
  - 文件夹预检不再向用户展示尚未硬执行的 `5000 文件 / 50 MiB` 软预算警告；可读数量与体积仍保留在报告中，敏感文件拦截继续明确展示。
- **状态：** [ ] （首屏气质与整体 polish 仍未完；上传反馈 + Copilot 意图已修，等你点验）

### 2b. 找不到「跟 Agent 说话」的地方（入口缺口 · 2026-07-17）

- **问题：** Owner 想用自然语言驱动画布（「昨天考察的项目」「凭什么」），但界面上**找不到对话入口**。
  - 顶栏 **AI Copilot** ≠ 对话框（历史/现状易变成跳重点，不像聊天）
  - 底栏时间线 **Agent** 只是事件筛选，不是对话
  - 真输入在 `AgentChatPanel`（右侧 Inspector），且 **仅当** 项目已有本地夹 `agentSession` 时才挂载；无 grant 的项目整块消失
  - 画布意图 / `set_canvas_view` / 黄金口令已落地，**缺的是常驻可见的说话入口**
- **你要看到的结果：**
  1. 任意已打开项目，**一眼能看到**「问 Agent」输入（不靠猜 tab、不靠先授权才出现）
  2. 无本地夹时：输入框仍在，提示先授权夹；有夹时：发送即 `ownerUtterance` → 工具环 → 画布按意图切换
  3. 顶栏 AI Copilot 点一下 = 聚焦/打开该对话区，而不是只跳材料
  4. 底栏「Agent」不与「对话」混淆（文案或拆开）
- **工程进展（2026-07-17）：**
  - 任意已打开项目：右侧 **「问 Agent」常驻**（不再依赖 `agentSession` 才挂载）
  - 无本地夹：输入框仍在 +「待授权」徽标；发送提示先授权
  - 有夹：发送 → `ownerUtterance` → 工具环 / 画布意图
  - 顶栏 **AI Copilot** → 聚焦右侧对话输入（`agentChatFocusKey`）
  - 底栏筛选文案改为 **「Agent 动态」**（时间线记录，不是对话）
  - 样式对齐 `project-canvas`（`--pc-accent` / 卡片圆角阴影）
  - 冷启动、刷新和切换项目时从真实 SourceGrant 恢复 Agent 会话，不再把已授权项目误标成「待授权」；真正未授权时，右栏直接提供「选择并授权文件夹」并进入既有预检确认流程。
- **工程线索：**
  - `AgentChatPanel` · `AgentPresenceRail` · `page.tsx` `openFolderAgentCopilot` / `onAgentChatSend`
  - 画布菜单：`docs/product/CANVAS_MENU_V1.md` · `shared/knowledge/canvas-intent.ts`
- **状态：** [ ] （工程入口已常驻；等你点验气质与授权后发送）

### 3. 你亲手用过并点头

- **品牌定案（2026-07-17）：** 参赛队伍「不俟终日」；作品与桌面 App「知几」；本机目录与 GitHub 仓库 slug `zhiji`。
- **问题：** 工程自测 / WebBridge 不能代替你用。
- **比赛版唯一任务：** 重新进入一个复杂项目后，无需重读整个项目，Agent 给出一份有证据的当前判断，并帮助你完成一个下一步决定。
- **结果：** 你在桌面 `.app` 或 3331 走通：最近项目 → 授权真夹 → map/search/read 真实过程 → 项目情报简报 → Claim 精确依据 → 接受/修改/拒绝/暂缓 → 刷新仍在；你说「过」才算过。
- **故事与工程合同：** `docs/product/交付叙事与Agent业务逻辑.md` · `docs/product/项目情报Agent比赛版开发说明.md`
- **工程进展：** 右栏简报 + grounded 硬门；WorkSuggestion 不自动建 todo；Claim 四态。Demo readiness：真实快照 + 真模型黄金路径 + 桌面重包。
  - 项目变化情报 Demo：冷启动恢复已授权文件夹观察；本次打开后新增/修改/移除的真实文件会触发一次 `source_change` Run。底部时间线只读展示「文件变化 → Agent 候选判断 → Owner 裁决」，不写假事件、不自动接受或建任务；模型失败不显示 Agent 结果。
- **状态：** [ ] **live-verified / packaged / ready-for-owner-recording**（**未** accepted，须 Owner 亲手录屏确认）
- **工程证据：** `.ship/evidence/demo-golden-path-c907db22/` · 快照 `/Users/mahaoxuan/Desktop/黑客松/fc-opc-demo-snapshot-1cbfa244` · 品牌候选包 `out/zhiji-logo-candidate/知几-darwin-arm64/知几.app`

### 3b. 比赛版桌面客户端（Electron 最小壳）

- **问题：** 演示不能要求先 `npm run dev`；网页层不能单独承担本机生命周期与 Finder 启动。
- **结果：** arm64 `.app` 监督只绑 `127.0.0.1` 的 Next standalone，固定打开 `/track/knowledge`；数据在 `userData/knowledge`；Renderer 沙箱；不打包密钥。授权/真读/证据/确认/失败语义不变。
- **开发说明：** `docs/product/桌面客户端比赛版开发说明.md` · 模型配置：`docs/product/DESKTOP_ENV_SETUP.md`
- **工程证据：** `desktop/*` · `scripts/desktop-*` · `scripts/install-desktop-env.mjs` · `output: standalone` · 单测 `tests/unit/desktop-*`
- **修复（Codex 复验项）：** utilityProcess 最小 env（无 `...process.env`）· 禁止一切新窗口 · 本地校验 Electron zip 可连打两次 package · **BYOK**：打包态只读用户自填的 userData `.env.local`，不预装 Key
- **进展：** 工作台内 BYOK 面板（顶栏钥匙 / 未配置黄条）→ `PUT /api/llm/byok` 写入本机文件并 `process.env` 即时生效；密钥不回显
- **状态：** [ ] **packaged**（2026-07-17 重包；冷启动 `127.0.0.1` + `/track/knowledge` + BYOK userData-file-only；**未** accepted）

### 3c. 模型连接器（多供应商选择）

- **问题：** 当前 BYOK 是网关、Key、模型名三个裸输入框，用户不知道支持谁，也无法从界面确认实际使用的供应商；如果只加 Logo 和下拉，会出现“看起来能选、运行时仍只走一种协议”的假能力。
- **结果：** 顶栏模型胶囊 → 带本地 Logo 的轻量下拉 → OpenAI / MiniMax / DeepSeek / 阶跃星辰 / 自定义网关；未配置项先测试真实连接，成功后才能保存并用于新 Run。Run Receipt 如实记录 provider / protocol / model；Key 仍只在本机且不回显。
- **开发说明：** `docs/product/模型连接器产品说明.md`
- **保护线：** 不覆盖现有 `ready-for-owner-recording` 兜底 `.app`；未真实适配或验证的供应商不得显示可用；模型别名不得冒充官方模型。
- **工程进展（安全收口 2026-07-17）：**
  - 服务端 `verifyAndActivate`：客户端 `verifiedAt` 拒绝；保存前再次真实 probe；原子写盘
  - 比赛主列表：`px_proxy` / `minimax_token_plan` / `stepfun_step_plan`；URL 精确校验；跨供应商禁复用 Key
  - Anthropic 顶层 `system`；thinking 不入正文；HTTP 200 空正文失败
  - Run 启动冻结 `LlmConnectionSnapshot`；Receipt 用同一 snapshot
  - package 默认拒绝覆盖兜底 `.app`（需 `DESKTOP_PACKAGE_OUT`）
- **Owner 可用连接：** PX Proxy（alias 带「PX 代理」）· MiniMax · Token Plan（官方）· 阶跃星辰 · Step Plan（官方）
- **信任边界收口（2026-07-17）：** 未 verified 不得调 LLM · credential fingerprint 绑定 Key · Run 冻结 snapshot · 模型级已连接 · 空正文抛 invalid_response · package 规范化拒绝 out/./out · saveByokSecrets 不能写 connected
- **状态：** [x] **implemented** / [x] **tested** / [x] **packaged**（**未** accepted；Owner 未亲手 live）
- **候选包：** `out/zhiji-logo-candidate/知几-darwin-arm64/知几.app`；旧品牌包只作历史兜底，不再作为交付包。

### 协作提醒（平级）

- **产品问题只写本文件**（`docs/product/产品清单.md`）。开发开干前先读「未完成」；改完在条目下补工程进展，不另起平行问题本。
- 当前主线：**§3 项目重入 Demo 垂直切片**；它收束 §1 / §1b 真读与过程、§2b 对话入口、§3b 桌面壳，不再扩新能力面。
- 唯一面：`/track/knowledge`；勿新建 `app/track/knowledge/mvp/`
- 夹具：`/tmp/mvp-v0-g6-d50-fixture`
- 证据目录：`.ship/evidence/`（`agent-e2e-*` · `agent-tool-loop-live.txt`）
- 画布呈现词表：`docs/product/CANVAS_MENU_V1.md`
- **vNext 工程（2026-07-17）：** PRD `docs/product/zhiji-vnext-optimization-prd.md`；执行状态只记在本清单，不另开平行板。可信修复已接成产品行为：所有本机 API 写入统一校验同端口 Origin + 会话 + CSRF；文件夹先展示实际读取范围、再由 Owner 明确确认连接；异步 Run 的 Owner 问句与队列落 SQLite，重启后的首个工作台读取会恢复队列，SSE 随真实工具收据更新；Claim 逐条裁决落 Project Memory SQLite，全部裁完才推进接受版本，右栏证据面板有独立空间、不遮挡 Inspector。仍缺：独立进程 Worker/完备 checkpoint、默认异步与全量 SQLite、Claim 全量替代 UnderstandingBody、红队/评测、Owner 点验。

---

## 已完成

### 0. 中心真节点（阶段 2）

- **问题：** 授权夹或拖入后，中心往往只剩空壳/「放进资料」引导，看不到材料·工作·理解依据构成的真实节点。
- **结果（BDD）：** 授权夹或拖入真实项目后 → 四区壳 → 中心=项目 → 周围 ≥3 个真实可点节点（材料/工作/理解依据）→ 点开右侧有摘要与依据。不是假占位。
- **定案 A（已废止自动进任务 · 2026-07-17）：** 材料与 Agent `nextDecision` **不得**自动创建 `status=todo` 正式任务。建议只出现在项目情报简报（suggestion）；须 Owner 明确采用后才建任务。`seed-work-items-from-materials` / agent-run writeback 已改为不新建 todo。
- **台账 / 验收：** `docs/product/GAP_TO_CANVASIGHT.md` · `docs/product/ACCEPTANCE_E7_E9.md`
- **状态：** [x] （Owner 浏览器点验通过：scion 真节点 + G6 Agent 动态/节点）

### 4. 进门责任反了

- **问题：** 要填 projectId、路径语法等。
- **结果：** 用户只点授权夹；系统在边界内理解；藏内部字段。
- **状态：** [x]

### 5. 产品是干什么的

- **问题：** 容易做成监控全机 / Coding 网关 / 纯搜文档。
- **结果：** 授权项目 → 有来源理解 → 回来重建状态 → 下一步决定。
- **状态：** [x]

### 6. 知识与信任

- **问题：** 假分数、新盖旧、检索变搜卡片。
- **结果：** 可追溯、不覆盖历史、事项状态重建为主。
- **状态：** [x]

### 7. 接入责任流

- **问题：** 授权 → 理解这条是否立得住。
- **结果：** 责任流验收过；**不代表**工具 Agent 已完成（见 §1 未完成）。
- **状态：** [x]

---

## 模板

```
### N. 标题
- **问题：**
- **结果：**
- **状态：** [ ]
```
